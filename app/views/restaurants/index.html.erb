<h1>Restaurants</h1>
<table class="table">
  <!-- Table header -->
  <tbody>
    <% @restaurants.each do |restaurant| %>
      <tr>
        <td><%= link_to restaurant.name, restaurant_path(restaurant) %></td>
      </tr>
    <% end %>
  </tbody>
</table>
